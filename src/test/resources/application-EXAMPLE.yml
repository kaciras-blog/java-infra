# 测试需要配置一些选项，可以把该文件改名为 application.yml，或者在启动时指定配置文件
spring:

  main:
    banner-mode: "off"

  session:
    store-type: redis
    timeout: 30m
    redis:
      namespace: test

  # 可能需要改为你自己的环境，默认值可用于CI
  redis:
    host: localhost
    port: 6379
