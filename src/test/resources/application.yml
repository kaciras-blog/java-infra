spring:

  main:
    web-application-type: servlet
    banner-mode: "off"

  session:
    store-type: redis
    timeout: 30m
    redis:
      namespace: test

# you should set following properties to your own

  redis:
    host: localhost
    port: 6379
    password: password
